import 'package:flutter/material.dart';

class QuizesSection extends StatelessWidget {
  const QuizesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Quizes Section')),
    );
  }
}
